{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api": "yarn workspace packages/api",
    "prepare": "husky install"
  },
  "dependencies": {
    "scripts": "^0.1.0"
  },
  "devDependencies": {
    "husky": "^7.0.0"
  },
  "lint-staged": {
    "**/*/*.{js,jsx,ts,tsx}": [
      "yarn eslint",
      "yarn test"
    ]
  }
}
